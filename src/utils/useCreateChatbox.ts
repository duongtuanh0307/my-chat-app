// import React from "react";
// import { useQuery, useMutation } from "@apollo/client";
// import { SEARCH_CHATBOX } from "../graphql/queries";
// import {  } from "../graphql/mutations";

// function useCreateChatbox(currentUserId: string, selectedGuestId: string) {
//   const userIdArr = [currentUserId, selectedGuestId];
//   const { loading, data } = useQuery(SEARCH_CHATBOX, {
//     variables: { userIdArr: userIdArr },
//   });

//   return 0;
// }
// export default useCreateChatbox;
const x = { y: 7 };
export default x;
